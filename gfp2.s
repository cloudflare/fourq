// func feAdd(c, a, b *gfP2)
TEXT ·feAdd(SB),0,$0-24
	MOVQ a+8(FP), DI
	MOVQ b+16(FP), SI
	MOVQ 0(DI), AX
	MOVQ 8(DI), BX
	MOVQ 16(DI), CX
	MOVQ 24(DI), DX
	ADDQ 0(SI), AX
	ADCQ 8(SI), BX
	BTRQ $63, BX
	ADCQ $0, AX
	ADCQ $0, BX
	ADDQ 16(SI), CX
	ADCQ 24(SI), DX
	BTRQ $63, DX
	ADCQ $0, CX
	ADCQ $0, DX
	MOVQ c+0(FP), DI
	MOVQ AX, 0(DI)
	MOVQ BX, 8(DI)
	MOVQ CX, 16(DI)
	MOVQ DX, 24(DI)
	RET

// func feSub(c, a, b *gfP2)
TEXT ·feSub(SB),0,$0-24
	MOVQ a+8(FP), DI
	MOVQ b+16(FP), SI
	MOVQ 0(SI), AX
	MOVQ 8(SI), BX
	MOVQ 16(SI), CX
	MOVQ 24(SI), DX
	NOTQ AX
	NOTQ BX
	BTRQ $63, BX
	NOTQ CX
	NOTQ DX
	BTRQ $63, DX
	ADDQ 0(DI), AX
	ADCQ 8(DI), BX
	BTRQ $63, BX
	ADCQ $0, AX
	ADCQ $0, BX
	ADDQ 16(DI), CX
	ADCQ 24(DI), DX
	BTRQ $63, DX
	ADCQ $0, CX
	ADCQ $0, DX
	MOVQ c+0(FP), DI
	MOVQ AX, 0(DI)
	MOVQ BX, 8(DI)
	MOVQ CX, 16(DI)
	MOVQ DX, 24(DI)
	RET
